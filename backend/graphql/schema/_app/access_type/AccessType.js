export const types = `
    type AccessType {
        companyKey: Company
        officeKey: Office
        roleKey: Role
        projectKey: Project
        accessLevel: Int
        externalId: [String]
        fullCount: Int
    }
`;

export const queries = `
`;

export const mutations = `

`;
