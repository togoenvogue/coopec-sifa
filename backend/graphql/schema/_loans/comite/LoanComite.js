export const types = `
    type LoanComite {
        _id: ID
        _key: ID
        loanFileKey: String
        userKey: String
        userFullName: String
        isPresident: Boolean
        signature: String
        fullCount: Int
    }
`;

export const queries = `

`;

export const mutations = `

`;
